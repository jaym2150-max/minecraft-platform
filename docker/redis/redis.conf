# Minecraft Platform — Redis Configuration
port 6379
appendonly yes
appendfsync everysec
save 900 1
save 300 10
save 60 10000
maxmemory 256mb
maxmemory-policy noeviction
# Password is set via docker-compose command line (--requirepass)
# requirepass is configured in docker-compose.yml for dev môi trường
