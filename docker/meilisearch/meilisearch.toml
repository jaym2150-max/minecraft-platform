# Minecraft Platform — Meilisearch Configuration
# Replace master_key with a secure value in production
env = "development"
master_key = "${MEILISEARCH_API_KEY}"
http_addr = "0.0.0.0:7700"
db_path = "/meili_data"
dump_dir = "/meili_data/dumps"
snapshot_dir = "/meili_data/snapshots"
