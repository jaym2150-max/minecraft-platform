# Minecraft Platform — MinIO Configuration
# Replace these with secure credentials in production
MINIO_ROOT_USER=${S3_ACCESS_KEY}
MINIO_ROOT_PASSWORD=${S3_SECRET_KEY}
MINIO_VOLUMES=/data
MINIO_OPTS="--console-address :9001"
